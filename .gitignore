.DS_Store
*.log
.vercel
node_modules/
dist/
.claude/settings.local.json

# Plaintext sources for passcode-gated pages. This repo is public — only the
# encrypted build output in public/plan/ may ever be committed.
private/
